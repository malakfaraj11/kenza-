// Seeders are permanently disabled. All catalogue data is dynamically structured and imported via OpenAI LLM parser.
export async function seed() {
  console.log('⚠️ Seeders are disabled in this project.');
}
